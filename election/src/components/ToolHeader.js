
export const ToolHeader = ({ headerText }) => {
    // const header = props.header;
    // const { header } = props;
    return (
            <header>
                <h1>{headerText}</h1>
            </header>
    );

};